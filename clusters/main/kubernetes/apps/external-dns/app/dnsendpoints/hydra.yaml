---
apiVersion: externaldns.k8s.io/v1alpha1
kind: DNSEndpoint
metadata:
  name: hydra-endpoint
  namespace: external-dns
spec:
  endpoints:
    - dnsName: hydra.${BASE_DOMAIN}
      recordType: A
      recordTTL: 300
      targets:
        - ${NGINX_EXTERNAL_IP}